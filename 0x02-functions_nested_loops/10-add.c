#include "main.h"
/**
*add - entry point(adds two integers and returns the result)
*@a: int to be added to b
*@b: int to be added to a
*Return: total of a+b
*/
int add(int a, int b)
{
int c;

c = a + b;
return (c);
}
